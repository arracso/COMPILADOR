// comenta ri

programa test
    const
        enter int: 2, int2: 4;
        real float: 4.233;
        boolea bool: Cert;
        car char: 'G';
    fconst
    var
        var_int, var_int2: enter;
        var_char, var_char2: car;
        var_bool: boolea;
        var_float: real;
    fvar
    
    var_int := int2;
    var_int2 := int;
    escriureln(var_int);
    escriureln(var_int2);
    llegir(var_int2);
    //escriureln(var_int2);
fprograma